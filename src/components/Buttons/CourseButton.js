import styled from 'styled-components'
import { colors } from '../../utils'

export const CourseButton = styled.button({
    background: colors.orange,
    border: 'none',
    padding: '0.4em',
    margin: '7px',
    borderRadius: '5px',
    color: 'white',
    cursor: 'pointer',
    boxShadow: 'grey 0 3px 6px'
})